import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.components.starter',
  appName: 'componentes',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;

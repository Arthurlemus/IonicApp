import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.noticias',
  appName: 'io.noticias.com',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
